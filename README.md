# RadGraph via Hugging Face — Streamlit demo

## Setup (local VS Code)

1. Copy `.env.example` to `.env` and set your `HUGGINGFACEHUB_API_TOKEN`.
2. Create a virtual environment:
   ```bash
   python -m venv .venv
   source .venv/bin/activate     # macOS / Linux
   .venv\Scripts\activate        # Windows PowerShell
